# Vite development proxy target; never put secrets in VITE_* variables.
API_PROXY_TARGET=http://127.0.0.1:5000

# Public backend origin for separate deployments; empty uses same-origin /api/v1.
VITE_API_BASE_URL=
